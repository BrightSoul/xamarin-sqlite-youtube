<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp1.Views.Menu" Title="Menu">
  <StackLayout>
  <Label Text="MENU" />
  <ListView ItemsSource="{Binding Pagine}" SelectedItem="{Binding PaginaSelezionata}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <TextCell Text="{Binding Nome}" Detail="{Binding Riepilogo}" />
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
  </StackLayout>
</ContentPage>